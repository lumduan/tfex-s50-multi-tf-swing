"""Write-back adapters for ``quant-infra-db`` and ``quant-api-gateway``.

The adapter modules land in Phase 0.4. This package is intentionally empty
in the early Phase 0.1 commit so the coverage gate has a valid target.
"""

from __future__ import annotations
